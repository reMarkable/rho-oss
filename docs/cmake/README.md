# rho's CMakeLists.txt support

These are the basics to writing a CMake buildsystem under rho.

## Helpers

This document is only for advanced users, and those who want to peek under the
hood. Take a look if you're interested, but it's almost certainly not important
if you don't want to write your own CMake libraries (and even then, it's
unlikely to be important). The document is [here][helpers].

[helpers]: helpers/README.md

